@model IEnumerable<NetPress.Models.OrderRebate>

@{
    ViewBag.Title = "Index";
}

<h2>订单返利管理</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Order.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Referee.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RebateValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isAuthorized)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Order.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Referee.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RebateValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isAuthorized)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.OrderRebateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrderRebateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrderRebateId })*@
        </td>
    </tr>
}

</table>
