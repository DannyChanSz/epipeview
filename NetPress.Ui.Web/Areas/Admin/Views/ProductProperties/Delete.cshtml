@model NetPress.Models.ProductProperty

@{
    ViewBag.Title = "Delete";
    string errorMsg = ViewBag.ErrorMsg == null ? "" : ViewBag.ErrorMsg;
}

<h2>删除</h2>

@*<h3>Are you sure you want to delete this?</h3>*@
<div>
    <h4>分类拓展</h4>
    <hr />
    <input type="hidden" id="hdErrorMsg" value="@errorMsg">
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ExpandProperty.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ExpandProperty.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ProductCategory.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ProductCategory.Name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="删除" class="btn btn-default" /> |
            @Html.ActionLink("返回", "CategoryProductProperties", new { id = Model.ProductCategoryId })
        </div>
    }
</div>
<script src="/Content/js/admin/showErrorMsg.js"></script>