@model IEnumerable<NetPress.Models.ProductPropertyMapping>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", new { id = item.ProductPropertyMappingId })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Product.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductProperty.ProductPropertyId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductProperty.ProductPropertyId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductPropertyMappingId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductPropertyMappingId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductPropertyMappingId })
        </td>
    </tr>
}

</table>
