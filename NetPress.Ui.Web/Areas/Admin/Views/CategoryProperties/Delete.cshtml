@using NetPress.Models;
@model NetPress.Models.CategoryProperty

@{
    ProductCategory productCategory = ViewBag.Category;
}

<h2>删除</h2>


<div>
    <h4>分类默认拓展属性</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ExpandProperty.name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ExpandProperty.name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ProductCategory.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ProductCategory.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Provalue)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Provalue)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="删除" class="btn btn-default" /> |
            @Html.ActionLink("返回", "Index", new { categoryId = productCategory.ProductCategoryId })
        </div>
    }
</div>
